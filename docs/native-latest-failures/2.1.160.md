# Native latest candidate failure: 2.1.160

- Run: https://github.com/taekchef/claude-code-zh-cn/actions/runs/26802264137
- Head SHA: `f38125b922b391da75f824f83018a96ebcd398a0`
- Status: `fail`
- Candidate kind: `native`
- Native: `darwin-arm64` / `ok` / `ok` / `ok`
- Display audit: `fail` (6 issues / 11 commands)

## What Failed

- Candidate verification did not pass: `status=fail`.
- Display audit did not pass: `status=fail`.
- Summary: pass=0, fail=1, skip=0.

## Display Audit Issues

- `top_help_line_51` (`top_help`, display-untranslated-line)

  ```text
  --fallback-model <model>              Enable automatic fallback to specified
  ```

- `top_help_line_52` (`top_help`, display-untranslated-line)

  ```text
  model(s) when the default model is
  ```

- `top_help_line_53` (`top_help`, display-untranslated-line)

  ```text
  overloaded or not available. Accepts a
  ```

- `top_help_line_54` (`top_help`, display-untranslated-line)

  ```text
  comma-separated list to try each in
  ```

- `top_help_line_55` (`top_help`, display-untranslated-line)

  ```text
  order. Re-tries the primary at the start
  ```

- `top_help_line_56` (`top_help`, display-untranslated-line)

  ```text
  of each user turn. (only works with
  ```

## Text Diff Excerpt

```markdown
# Upstream text diff: 2.1.159 -> 2.1.160

- Added upstream strings: 4434
- Removed upstream strings: 4093
- Already covered by translations: 4
- Needs translation review: 4430
- Sensitive review hints: 91

## Added strings needing review
- - **CONFIRMED** \u2014 can name the inputs/state that trigger it and the wrong output or crash. Quote the line. - **PLAUSIBLE** \u2014 mechanism is real, trigger is uncertain (timing, env, config). State what would confirm it. - **REFUTED** \u2014 factually wrong (code doesn't say that) or guarded elsewhere. Quote the line that proves it.
- - Escape character is backtick (`), not backslash - Use Verb-Noun cmdlet naming: Get-ChildItem, Set-Location, New-Item, Remove-Item - Common aliases: ls (Get-ChildItem), cd (Set-Location), cat (Get-Content), rm (Remove-Item) - Pipe operator | works similarly to bash but passes objects, not text - Use Select-Object, Where-Object, ForEach-Object for filtering and transformation - String interpolation:
- - Registry access uses PSDrive prefixes: `HKLM:\SOFTWARE\...`, `HKCU:\...` \u2014 NOT raw `HKEY_LOCAL_MACHINE\...` - Environment variables: read with `$env:NAME`, set with `$env:NAME =
- --basic-regexp
- --color=auto
- --extended-regexp
- --fixed-strings
- --ignore-case
- --line-number
- --line-regexp
- --no-filename
- --perl-regexp
- --with-filename
- --word-regexp
- -algorithm
- -all
- -allcentralaccesspolicies
- -allmatches
- -allowinsecureredirect
- -allowunencryptedauthentication
- -append
- -argumentlist
- -asbytestream
- -AsHashtable
- -attributes
- -audit
- -authentication
- -body
- -casesensitive
- -certificate
- -certificatethumbprint
- -cimsession
- -com
- -comobject
- -compressionlevel
- -computername
- -confirm
- -container
- -content
- -contenttype
- ... 4390 more

## Added strings already covered
- Completing auth flow with authorization code
- Preserved step-up auth state across revocation
- Received auth code via manual callback URL
- Server does not support token revocation

## Removed strings
- --align
- --attach
- --auth-type
- --boottime
- --chdir
- --chroot
- --class
- --classdata
- --close-from
- --columns
- --command
- --command-timeout
- --config
- --conflict-exit-code
- --const-print-style
- --cpu-list
- --detach-on
- --echo
- --env
- --equexit
- --expr
- --group
- --host
- --interruptible
- --interval
- --library
- --log-in
- --log-io
- --log-out
- --log-timing
- --logging-format
- --monotonic
- --other-user
- --output-limit
- --pgid
- --pid
- --prompt
- --propagation
- --role
- --root
- ... 4053 more
```

## Takeover Commands

```bash
node scripts/verify-upstream-compat.js --baseline 2.1.160 --skip-latest --native-macos-arm64 --json
node scripts/generate-upstream-text-diff.js --to 2.1.160 --native-macos-arm64
node scripts/promote-native-candidate.js --candidate <candidate-json>
```

## Done Criteria

- Add the missing source-of-truth translations or guard fix on this branch.
- Re-run the native latest candidate workflow for this version.
- Only promote support metadata after native verification and display audit pass.
- Remove or update this handoff report before marking the PR ready.
