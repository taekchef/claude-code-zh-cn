## 关联 issue

- Related to #
- 如果这个修复需要 reporter 复测，请保持 issue open；PR 描述里不要写 GitHub 自动关闭关键字加 issue 编号。

## 提交前检查

> CI 会自动运行 `bash scripts/preflight.sh`，PR 描述里不需要手动勾选这个检查。
> 
> 如果是 fork PR，需要权限的 CI 检查会由维护者在 review 后触发。
